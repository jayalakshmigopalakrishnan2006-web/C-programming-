#include <stdio.h>

int main()
{
    int a;

    scanf("%d", &a);

    if (a < 12)
    {
        printf("Child discount");
    }
    else if (a >= 60)
    {
        printf("Senior discount");
    }
    else
    {
        printf("No discount");
    }

    return 0;
}
